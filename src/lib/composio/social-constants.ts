/**
 * Cookie compartilhado entre a action que inicia a conexao de uma rede
 * social (Composio) e a rota de callback -- mesmo padrao de
 * `INSTAGRAM_CONNECT_COOKIE`. Guarda `{connectionId, platform, label}`.
 */
export const SOCIAL_CONNECT_COOKIE = "composio-social-connection";
